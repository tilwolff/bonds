var MarketData=function(valdate){
        this._valdate=valdate;
        this._current_curves=[];
        this._scenarios=[];
}

MarketData.prototype.addCurve=function(type, times, values, tag_currency, tag_class, tag_id){
        
}

